# AIC 多模态目标检测：云服务器环境配置与训练运行手册

本文档用于在单卡 NVIDIA GPU 云服务器上复现本项目已经验证过的
Python、PyTorch、CUDA 和 Ultralytics 环境，并依次完成冒烟测试、第一阶段训练、
高分辨率微调、全量训练、测试集推理和提交文件打包。

文档以当前本地已验证环境为基准：

- Ubuntu 24.04（云服务器使用 Ubuntu 22.04 或 24.04 均可）
- Python 3.10.20
- PyTorch 2.11.0+cu128
- TorchVision 0.26.0+cu128
- Ultralytics 8.4.106
- CUDA Runtime 12.8
- 单张 NVIDIA GPU

> 所有训练命令都必须在项目根目录执行。不要使用系统 Python，也不要使用
> `sudo pip`。比赛数据只能通过私人方式传输到服务器，禁止上传到公开 GitHub 仓库。

## 1. 推荐服务器规格

推荐租用以下配置：

| 项目 | 推荐配置 |
| --- | --- |
| GPU | 单张 RTX 4090 24GB |
| CPU | 8 核或以上 |
| 内存 | 32GB 或以上 |
| 系统盘 | 30GB 或以上 |
| 数据盘 | 100GB 或以上 |
| 系统 | Ubuntu 22.04/24.04 x86_64 |
| 驱动 | `nvidia-smi` 显示支持 CUDA 12.8 |

当前训练代码使用 `device: 0`，按照单卡运行设计。不要为了加速直接租用多张 GPU，
因为当前配置尚未针对多卡 DDP 做完整验证。

如果平台提供预装镜像，优先选择：

```text
PyTorch 2.x + Python 3.10 + CUDA 12.8
```

镜像内的 PyTorch 版本不一致也没有关系，后文会在独立 Conda 环境中安装固定版本。
如果 `nvidia-smi` 显示主机驱动最高只支持低于 CUDA 12.8 的版本，建议更换主机或镜像，
不要自行替换云平台宿主机驱动。

## 2. 云端目录约定

AutoDL 的数据盘通常是 `/root/autodl-tmp`。本文统一把项目放在：

```text
/root/autodl-tmp/aic
```

其他平台可以换成其高速数据盘目录，但之后的命令和数据集 YAML 路径必须同步修改。
不要把 17GB 的处理后数据放在容量较小的系统盘。

预期目录结构：

```text
/root/autodl-tmp/aic/
├── aic_mm/
│   └── data/
├── configs/
├── scripts/
├── tests/
├── data/
│   └── processed/
├── weights/
│   └── yolo26s.pt
└── outputs/
```

## 3. 第一次开机：检查服务器

进入服务器终端后运行：

```bash
nvidia-smi
uname -m
df -h
free -h
```

检查结果：

- `uname -m` 应为 `x86_64`。
- `nvidia-smi` 必须能够识别 NVIDIA GPU。
- `nvidia-smi` 右上角显示的 CUDA Version 应不低于 12.8。
- 数据盘剩余空间建议不少于 60GB；如需同时保存原始数据，建议不少于 100GB。
- 内存建议不少于 32GB。

确认 Git 和 Conda：

```bash
git --version
conda --version
```

如果平台终端尚未加载 Conda，常见加载命令是：

```bash
source /root/miniconda3/etc/profile.d/conda.sh
```

如果镜像中没有 Git 或 tmux，可在拥有 root 权限时安装：

```bash
apt-get update
apt-get install -y git tmux
```

## 4. 克隆项目代码

```bash
cd /root/autodl-tmp
git clone https://github.com/lhc14723/aic.git
cd /root/autodl-tmp/aic
```

必须检查多模态数据加载源码是否存在：

```bash
test -f aic_mm/data/dataset.py
test -f aic_mm/data/encode.py
test -f aic_mm/data/manifest.py
test -f aic_mm/data/split.py
```

以上命令没有输出且返回终端提示符表示文件存在。如果出现错误，说明 GitHub 仓库尚未
包含 `aic_mm/data/` 源码，此时不能开始配置训练。

查看当前代码版本：

```bash
git status
git log -1 --oneline
```

## 5. 创建独立 Conda 环境

加载 Conda：

```bash
source /root/miniconda3/etc/profile.d/conda.sh
```

创建 Python 3.10.20 环境：

```bash
conda create -n aic-mm python=3.10.20 pip -y
conda activate aic-mm
```

确认终端提示符前出现 `(aic-mm)`，然后检查：

```bash
which python
python --version
python -m pip --version
```

期望：

```text
Python 3.10.20
```

如果平台的软件源暂时找不到精确的 `3.10.20`，可以退回 Python 3.10 的最新补丁版本：

```bash
conda create -n aic-mm python=3.10 pip -y
```

不要使用 Python 3.12 或 3.13 替代，因为项目当前没有在这些版本上验证。

## 6. 安装 PyTorch CUDA 12.8

必须在 `(aic-mm)` 环境中执行：

```bash
python -m pip install \
  torch==2.11.0 \
  torchvision==0.26.0 \
  --index-url https://download.pytorch.org/whl/cu128
```

PyTorch CUDA wheel 已包含所需 CUDA Runtime 和 cuDNN 用户态库。通常不需要另外安装
完整 CUDA Toolkit，也不要在 Conda 环境中再混装另一套 `pytorch-cuda`。

安装后立即检查：

```bash
python -c "import torch; print('torch:', torch.__version__); print('cuda:', torch.cuda.is_available()); print('torch cuda:', torch.version.cuda); print('gpu:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'NONE')"
```

期望至少包含：

```text
torch: 2.11.0+cu128
cuda: True
torch cuda: 12.8
```

如果 `cuda` 为 `False`，不要继续安装其他包或开始训练，先检查云服务器 GPU、驱动和镜像。

## 7. 安装项目 Python 依赖

以下是当前本地已经验证的核心运行和测试依赖。使用一个命令安装：

```bash
python -m pip install \
  ultralytics==8.4.106 \
  numpy==2.2.6 \
  opencv-python==5.0.0.93 \
  Pillow==12.2.0 \
  PyYAML==6.0.3 \
  scipy==1.15.3 \
  ImageHash==4.3.2 \
  matplotlib==3.10.9 \
  requests==2.34.2 \
  psutil==7.2.2 \
  polars==1.43.0 \
  nvidia-ml-py==13.610.43 \
  ultralytics-thop==2.0.20 \
  filelock==3.29.0 \
  typing-extensions==4.15.0 \
  sympy==1.14.0 \
  networkx==3.4.2 \
  Jinja2==3.1.6 \
  fsspec==2026.4.0 \
  PyWavelets==1.8.0 \
  pytest==9.1.1
```

各主要包的用途：

| 包 | 已验证版本 | 用途 |
| --- | --- | --- |
| torch | 2.11.0+cu128 | GPU 训练与推理 |
| torchvision | 0.26.0+cu128 | PyTorch 视觉依赖 |
| ultralytics | 8.4.106 | YOLO26 模型、训练器和推理 |
| numpy | 2.2.6 | 多模态数组处理和指标计算 |
| opencv-python | 5.0.0.93 | RGB、红外、深度和 TIFF 读写 |
| Pillow | 12.2.0 | 图像及 ImageHash 依赖 |
| PyYAML | 6.0.3 | 训练配置读取 |
| scipy | 1.15.3 | 数据划分整数规划相关计算 |
| ImageHash | 4.3.2 | 近重复图像哈希检查 |
| matplotlib | 3.10.9 | Ultralytics 训练图表 |
| requests | 2.34.2 | Ultralytics 下载和网络依赖 |
| psutil | 7.2.2 | 系统资源检测 |
| polars | 1.43.0 | Ultralytics 结果处理 |
| nvidia-ml-py | 13.610.43 | GPU 状态读取 |
| ultralytics-thop | 2.0.20 | 模型计算量统计 |
| pytest | 9.1.1 | 项目单元测试 |

PyTorch wheel 会自动安装匹配的 CUDA、cuDNN、NCCL、Triton 等底层依赖，不要单独下载
或覆盖这些包。当前本地环境中关键底层版本为：

| 包 | 版本 |
| --- | --- |
| triton | 3.6.0 |
| cuda-bindings | 12.9.4 |
| cuda-toolkit | 12.8.1 |
| nvidia-cudnn-cu12 | 9.19.0.56 |
| nvidia-nccl-cu12 | 2.28.9 |

这些底层包应由 PyTorch 的 cu128 软件源自动解析，不建议逐个手工安装。

## 8. 完整环境验证

```bash
python -c "import cv2, imagehash, numpy, scipy, torch, torchvision, ultralytics, yaml; print('imports: OK'); print('python torch:', torch.__version__); print('torchvision:', torchvision.__version__); print('ultralytics:', ultralytics.__version__); print('opencv:', cv2.__version__); print('numpy:', numpy.__version__); print('scipy:', scipy.__version__); print('cuda:', torch.cuda.is_available())"
```

运行 Ultralytics 环境检查：

```bash
yolo checks
```

运行项目测试：

```bash
cd /root/autodl-tmp/aic
PYTHONDONTWRITEBYTECODE=1 python -m pytest -q
```

当前项目的预期测试结果为：

```text
7 passed
```

如果出现：

```text
ModuleNotFoundError: No module named 'aic_mm.data'
```

说明 GitHub 仓库缺少 `aic_mm/data/` 源码，不是 Python 环境问题。

## 9. 准备公开预训练权重

创建权重目录：

```bash
cd /root/autodl-tmp/aic
mkdir -p weights
```

方法一：把本地已经验证的 `weights/yolo26s.pt` 私下传到云服务器的相同路径。

方法二：通过 Ultralytics 官方资源下载器下载：

```bash
python -c "from ultralytics.utils.downloads import attempt_download_asset; print(attempt_download_asset('weights/yolo26s.pt'))"
```

确认文件存在：

```bash
ls -lh weights/yolo26s.pt
```

本地已验证文件大小约为 20MB。如果文件不存在，第一阶段训练会直接报错，不会自动退回
其他模型。

## 10. 数据上传后的路径设置

处理后的数据应放在：

```text
/root/autodl-tmp/aic/data/processed
```

至少包含：

```text
data/processed/
├── aic_multispectral.yaml
├── aic_multispectral_all.yaml
├── images/
│   ├── train/
│   ├── val/
│   └── test/
└── labels/
    ├── train/
    └── val/
```

当前数据集 YAML 是本地生成的，里面的 `path` 可能仍指向 WSL 的绝对路径。上传后运行：

```bash
cd /root/autodl-tmp/aic
sed -i 's#^path: .*#path: /root/autodl-tmp/aic/data/processed#' data/processed/aic_multispectral.yaml
sed -i 's#^path: .*#path: /root/autodl-tmp/aic/data/processed#' data/processed/aic_multispectral_all.yaml
```

检查：

```bash
head -n 8 data/processed/aic_multispectral.yaml
head -n 10 data/processed/aic_multispectral_all.yaml
find data/processed/images/train -type f | wc -l
find data/processed/images/val -type f | wc -l
find data/processed/images/test -type f | wc -l
```

预期图像数量：

```text
train: 1600
val:    400
test:  1000
```

不要修改测试集内容、人工标注测试集或人工编辑测试预测结果。

## 11. 每次服务器开机或新开终端必须执行

下面这组命令不是只执行一次，而是每次新开终端都要执行：

```bash
source /root/miniconda3/etc/profile.d/conda.sh
conda activate aic-mm
cd /root/autodl-tmp/aic
export CUDA_VISIBLE_DEVICES=0
export PYTHONUNBUFFERED=1
```

然后快速检查：

```bash
python --version
python -c "import torch; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0))"
pwd
```

正确状态应为：

- 终端提示符以 `(aic-mm)` 开头。
- Python 为 3.10.20。
- PyTorch 为 2.11.0+cu128。
- `torch.cuda.is_available()` 为 `True`。
- 当前目录为 `/root/autodl-tmp/aic`。

## 12. 正式训练前的冒烟测试

冒烟测试只训练一轮、只使用少量数据，用于检查代码、数据、权重和 CUDA 是否能够完整跑通：

```bash
python -m scripts.train --config configs/smoke_train.yaml --yes
```

另开一个终端监控 GPU：

```bash
watch -n 2 nvidia-smi
```

冒烟测试必须满足：

- 没有 `ModuleNotFoundError`。
- 没有数据路径错误。
- 没有 TIFF 通道数错误。
- 没有 CUDA OOM。
- GPU 利用率能在训练阶段明显升高。
- 能完成训练和验证。

冒烟测试成功前不要启动 180 轮正式训练。

## 13. 使用 tmux 防止 SSH 断开导致训练停止

创建会话：

```bash
tmux new -s aic-train
```

进入 tmux 后再次执行本章第 11 节的环境激活命令，然后运行训练。

临时离开 tmux：

```text
先按 Ctrl+B，松开后再按 D
```

重新连接：

```bash
tmux attach -t aic-train
```

查看已有会话：

```bash
tmux ls
```

## 14. 三阶段正式训练命令

### 阶段一：固定训练集和验证集

默认配置：

- 1600 张训练图
- 400 张验证图
- 180 epochs
- `imgsz=960`
- `batch=4`
- `patience=40`
- AMP 开启

启动：

```bash
python -m scripts.train --config configs/train_fusion.yaml --yes
```

输出：

```text
outputs/aic_fusion_stage1/
```

主要权重：

```text
outputs/aic_fusion_stage1/weights/best.pt
outputs/aic_fusion_stage1/weights/last.pt
```

阶段一包含早停：验证指标连续 40 轮没有改善时可以提前停止。训练完成后应检查
`best.pt` 的验证结果，而不是只看最后一轮。

### 阶段二：1280 高分辨率微调

只有阶段一成功生成 `best.pt` 后才能运行：

```bash
python -m scripts.train --config configs/finetune_highres.yaml --yes
```

默认配置：

- 使用阶段一 `best.pt`
- 45 epochs
- `imgsz=1280`
- `batch=1`
- `patience=20`

输出：

```text
outputs/aic_fusion_highres/
```

主要权重：

```text
outputs/aic_fusion_highres/weights/best.pt
outputs/aic_fusion_highres/weights/last.pt
```

### 阶段三：全部 2000 张训练图短程收敛

只有在模型结构和参数全部冻结后才能运行：

```bash
python -m scripts.train --config configs/final_all_data.yaml --yes
```

默认配置：

- 使用高分辨率阶段 `best.pt`
- 训练集包含原 train 和 val，共 2000 张图
- 30 epochs
- `imgsz=1280`
- `batch=1`
- 关闭验证

输出：

```text
outputs/aic_fusion_final_all/
```

最终默认推理权重：

```text
outputs/aic_fusion_final_all/weights/last.pt
```

第三阶段关闭了验证，因此不能根据测试集表现继续反向调整参数。

## 15. 24GB GPU 的 batch 调整原则

现有正式配置是为 8GB RTX 5060 Laptop GPU 编写的，在 RTX 4090 24GB 上可以直接运行，
但可能不能充分利用显存。

为了保证首次云端运行稳定，建议先保持原配置完成冒烟测试。确认没有错误后，再复制配置
文件进行 batch 探测，不要直接覆盖原始配置。参考起点：

| 阶段 | 8GB 已验证值 | 24GB 建议测试起点 |
| --- | ---: | ---: |
| 960 第一阶段 | 4 | 10 或 12 |
| 1280 高分辨率 | 1 | 3 或 4 |
| 1280 全量阶段 | 1 | 3 或 4 |

同时可以把 `workers` 从 1 逐步提高到 4。每次只修改 batch 和 workers，并先进行一轮小规模
测试。GPU 峰值显存建议保留 10% 至 15% 余量。

如果出现 CUDA OOM：

1. 先减小 batch。
2. 关闭占用同一张卡的其他进程。
3. 保持 AMP 开启。
4. 不要第一时间降低 960/1280 图像尺寸，因为小目标检测对分辨率敏感。

增大 batch 主要用于提高吞吐，并不能直接保证提高 mAP。

## 16. 训练中断与断点保护

训练过程中不要只依赖云服务器本地盘。至少定期备份：

```text
outputs/*/weights/best.pt
outputs/*/weights/last.pt
outputs/*/results.csv
configs/
```

如果训练意外中断，不要直接删除输出目录或从头开始。先确认：

```bash
ls -lh outputs/aic_fusion_stage1/weights/
```

如果 `last.pt` 完整存在，可复制原配置创建恢复配置，并在 `train:` 下加入：

```yaml
resume: /root/autodl-tmp/aic/outputs/aic_fusion_stage1/weights/last.pt
```

然后使用恢复配置运行。恢复前应确认配置指向的阶段和 checkpoint 一致，避免拿高分辨率
checkpoint 恢复 960 阶段，或覆盖已完成的结果目录。

## 17. 测试集自动推理和提交打包

确认 `configs/predict.yaml` 默认权重为：

```text
outputs/aic_fusion_final_all/weights/last.pt
```

执行自动推理：

```bash
python -m scripts.predict --config configs/predict.yaml
```

执行格式校验和 ZIP 打包：

```bash
python -m scripts.package_submission
```

预期输出：

```text
outputs/test_predictions/
outputs/submission.zip
```

打包脚本会自动检查：

- 恰好 1000 个预测 TXT。
- 每行六列。
- 类别、坐标和置信度范围合法。
- 每张图目标数不超过限制。
- 结果按置信度排序。
- ZIP 根目录直接包含预测 TXT。

不得人工补框、删框、改框或依据测试图内容选择结果。

## 18. 常见错误

### `No module named 'aic_mm.data'`

GitHub 仓库缺少 `aic_mm/data/` 源码。重新提交该目录并在服务器执行：

```bash
git pull
```

### `Processed dataset YAML not found`

检查当前目录和数据目录：

```bash
pwd
ls -lh data/processed/aic_multispectral.yaml
```

### 数据 YAML 仍指向本地 WSL

检查：

```bash
head -n 3 data/processed/aic_multispectral.yaml
```

把 `path` 改为云服务器上的绝对路径。

### `Pretrained checkpoint not found`

检查：

```bash
ls -lh weights/yolo26s.pt
```

### `torch.cuda.is_available()` 为 `False`

依次检查：

```bash
nvidia-smi
which python
python -m pip show torch
python -c "import torch; print(torch.__version__, torch.version.cuda)"
```

确认当前终端已经激活 `(aic-mm)`，并且安装的是 cu128 wheel，而不是 CPU wheel。

### CUDA out of memory

降低当前阶段配置中的 `batch`，不要同时修改分辨率、学习率和增强参数。

### 训练进程显示 `Killed`

通常是系统内存超限而不是显存不足。降低 `workers`，并检查：

```bash
free -h
```

## 19. 环境留档

环境验证成功后保存版本快照：

```bash
mkdir -p outputs/environment
python --version > outputs/environment/python-version.txt
python -m pip freeze > outputs/environment/pip-freeze.txt
nvidia-smi > outputs/environment/nvidia-smi.txt
git rev-parse HEAD > outputs/environment/git-commit.txt
```

训练结束后将 `outputs/environment/` 与最终权重一并下载备份。这样即使原云实例释放，也能
追溯代码提交、Python 包和 GPU 驱动环境。

## 20. 最短执行清单

第一次配置：

```bash
cd /root/autodl-tmp
git clone https://github.com/lhc14723/aic.git
source /root/miniconda3/etc/profile.d/conda.sh
conda create -n aic-mm python=3.10.20 pip -y
conda activate aic-mm
cd /root/autodl-tmp/aic
```

随后按照第 6、7 节安装固定版本依赖。

每次重新登录：

```bash
source /root/miniconda3/etc/profile.d/conda.sh
conda activate aic-mm
cd /root/autodl-tmp/aic
export CUDA_VISIBLE_DEVICES=0
export PYTHONUNBUFFERED=1
```

数据和权重准备完成后：

```bash
PYTHONDONTWRITEBYTECODE=1 python -m pytest -q
python -m scripts.train --config configs/smoke_train.yaml --yes
python -m scripts.train --config configs/train_fusion.yaml --yes
python -m scripts.train --config configs/finetune_highres.yaml --yes
python -m scripts.train --config configs/final_all_data.yaml --yes
python -m scripts.predict --config configs/predict.yaml
python -m scripts.package_submission
```

三段正式训练必须依次完成，不要同时启动。每进入下一阶段前，先确认上一阶段的
`best.pt` 已正常生成并完成备份。
